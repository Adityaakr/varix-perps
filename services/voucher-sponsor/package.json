{
  "name": "@varix/voucher-sponsor",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "node --test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
