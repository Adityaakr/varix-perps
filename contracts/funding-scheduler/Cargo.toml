[package]
name = "funding-scheduler"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Funding cadence coordinator scaffold for Varix"
repository.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
funding-scheduler-app.workspace = true
sails-rs.workspace = true

[build-dependencies]
funding-scheduler-app.workspace = true
sails-rs = { workspace = true, features = ["build"] }

[workspace]
resolver = "3"
members = ["app", "client"]

[workspace.package]
version = "0.1.0"
authors = ["OpenAI"]
edition = "2024"
rust-version = "1.91"
repository = "https://github.com/adityakrx/varix"
license = "MIT"
keywords = ["vara", "gear", "sails", "perps", "funding"]
categories = ["cryptography::cryptocurrencies", "no-std", "wasm"]

[workspace.dependencies]
funding-scheduler-app = { version = "0.1.0", path = "app" }
funding-scheduler-client = { version = "0.1.0", path = "client" }
sails-rs = "0.10.3"
varix-shared = { version = "0.1.0", path = "../shared" }
